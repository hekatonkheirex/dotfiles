import QtQuick
import "primitives"
import "../config"

StatusIndicator {
  id: root

  iconLabel: "settings"
  accentColor: Colors.primary
  accessibleName: "Power Options"
  tooltipText: "Power Options"
}
