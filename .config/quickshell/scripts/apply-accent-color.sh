#!/bin/sh
# Kept as a compatibility entry point for older integrations. The active shell
# palette is authored in config/Colors.qml and cannot be replaced at runtime.
echo "The Material 3 Expressive palette is fixed; accent changes are disabled." >&2
exit 2
